#  When ./confgure is being run, options listed here are appended
#  after default Cygwin option set.

# --disable-nls
--with-libidn
--with-libgsasl

# SMTP programs are installed in here
--bindir=/usr/sbin

# Keyring, instead of gnome-keyring
--with-libsecret

--sysconfdir=/etc
--prefix=/usr
